
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>Paws & Claws</title>
    <!-- Favicon-->
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <!-- Bootstrap icons-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <!-- Core theme CSS (includes Bootstrap)-->
    <link href="css/style2.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap4.min.css">

    <style>
        body {
            margin: 0;
            padding: 0;
        }

        .card-body-fixed-height {

            max-height: 200px;
            /* Adjust the height as needed */
            overflow: hidden;
        }

        /* Style for the product details container */
        .product-details-container {
            display: flex;
            align-items: stretch;
            /* Make both cards stretch to equal height */
            justify-content: space-around;
            margin: 20px;
        }

        /* Style for the product image card (left side) */
        .product-image-card {
            flex: 1;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Style for the product image */
        .product-image {
            max-width: 100%;
            height: auto;
        }

        /* Style for the product details card (right side) */
        .product-details-card {
            flex: 1;
            /* Equal flex value to ensure same height as the image card */
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);

            /* height: 50%; */
        }

        /* Style for the main container */
        .main-container {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            /* Minimum height to fill the viewport */
        }

        /* Style for the product details container */
        .product-details-container {
            display: flex;
            flex: 1;
            /* Grow to fill available space */
            align-items: stretch;
            justify-content: space-around;
            margin: 20px;
        }

        /* Style for the product image card (left side) */
        .product-image-card {
            flex: 1;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Style for the product image */
        .product-image {
            max-width: 100%;
            height: auto;
        }

        /* Style for the product details card (right side) */
        .product-details-card {
            flex: 1;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Style for the footer */
        .user-footer {
            margin-top: auto;
            /* Push the footer to the bottom */
        }

        /* Define the zoom effect for the image */
        /* Style for the main container */
        #message {
            background-color: #c9c4c4;
            padding: 0;
            border-radius: 5px;
            /* margin: 20px auto; */
            max-width: 100%;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            position: relative;
            /* Add this to make positioning of the footer relative */
            height: 100vh;
        }

        /* Style for the footer container */
        .footer {
            background-color: #333;
            /* Background color */
            color: white;
            /* Text color */
            padding: 20px;
            /* Padding around content */
            text-align: center;
            /* Center align text */
            height: 100px;

        }

        /* Style for the footer content */
        .footer-content {
            max-width: 800px;
            /* Maximum width for content */
            margin: 0 auto;
            /* Center align content horizontally */


        }

        /* Style for the "Contact Us" heading */
        .footer-content p {
            font-weight: bold;
            /* Bold text */
            margin-bottom: 10px;
            /* Space below heading */
        }

        /* Style for the list items */
        .footer-content ul {
            list-style-type: none;
            /* Remove bullet points */
            padding: 0;
            /* Remove default padding */
        }

        /* Style for list items */
        .footer-content li {
            margin-bottom: 5px;
            /* Space between list items */
        }

        /* Hover effect for list items (optional) */
        .footer-content li:hover {
            color: #FFD700;
            /* Change text color on hover */
        }

        .navbar {
    display: flex;
    justify-content: left;
    align-items: center;
    padding-left: 100px;
}

.navbar-links {
    display: flex;
    justify-content: space-between;
    width: 100%;
}


        .bullet-point {
            list-style: none;
        }


        .marquee-container {
    width: 100%;
    overflow: hidden;
}

marquee {
    width: 100%;
    white-space: nowrap; /* Prevent line breaks */
}

.marquee-image {
    width: 1=70%; /* Default image width for larger screens */
}



@media (max-width: 768px) {
    /* Adjust image size for screens up to 768px width (typical mobile screens) */
    .marquee-image {
        width: 100%; /* You can adjust the percentage as needed */
        
    }
    
}

/* Add styles for mobile screens (max-width: 767px) */
@media (max-width: 767px) {
    .navbar-brand {
        font-size: 15px; /* Adjust the font size for mobile */
    }
    .navbar-nav {
        text-align: right;
        margin-right: 0;
        padding-right: 10px; /* Add some right padding for better alignment */
        margin-left: 0;
    }
    .navbar-brand {
        margin-right: auto; /* Push the brand/logo to the left */
    }
}

    </style>


</head>
<script>
    // Function to hide the marquee after 1 second
    function hideMarquee() {
        setTimeout(function () {
            var marquee = document.querySelector('marquee');
            marquee.style.display = 'none';
        }, 3000); // 1000 milliseconds (1 second)
    }
</script>

<body onload="hideMarquee()">


    <!---------------------------- extra -------------------------->


    <div class="progress" id="progress">
        <div id="progress-bar" class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar"
            aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%"></div>
        <div>

        </div>
    </div>
  

    <div class="marquee-container">
        <marquee behavior="scroll" direction="right" scrollamount="45">
            <img class="marquee-image" src="https://i.pinimg.com/originals/19/58/38/19583866f2c33d855fd2be99b47cddfe.gif" alt="Running Cat">
        </marquee>
    </div>
    
    
    

    <div id="message" style="display: none;" class="container-fluid " style="height: 100vh;">


        <!-- As a link -->
        <nav class="navbar navbar-light bg-light container-fluid">
            <a class="navbar-brand" href="/home">Paws&Claws</a>
            <a class="navbar-brand" href="/home">Home</a>
            <a class="navbar-brand" href="/cats">Cats</a>
            <a class="navbar-brand" href="/dogs">Dogs</a>
        </nav>
        
        
        

        <div class="container-fluid my-5  d-flex  justify-content-center " >
            <div class="card card-1">
                <div class="card-header bg-white">
                    <div class="media flex-sm-row flex-column-reverse justify-content-between  ">
                        <div class="col my-auto">
                            <h4 class="mb-0">Thanks for your Order <span class="change-color">
                                    <%= user.fullname %>
                                </span> !</h4>
                                
                        </div>
                        <div class="col-auto text-center  my-auto pl-0 pt-sm-4"> <img
                                class="img-fluid my-auto align-items-center mb-0 pt-3"
                                src="https://media1.tenor.com/images/895def190d60d2ed353446b1fe9ad7d4/tenor.gif?itemid=12387022"
                                width="115" height="115">
                            <!-- <p class="mb-4 pt-0 Glasses">Your Orders</p> -->
                        </div>
                       
                    </div>
                </div>
                <div class="card-footer bg-white">
                    <div class="text-center">
                        <a href="mailto:veshalbrototype@gmail.com" class="btn btn-primary">Contact Admin</a>
                    </div>
                </div>
                <div class="card-body ">
                    <table id="example" class="table table-responsive table-bordered" style="width:100%">
                        <thead>
                            <tr>
                                <th>Order Number </th>
                                <th>Product Name</th>
                                <th>Price</th>
                                <th>Quantity</th>
                                <th>Amount Paid</th>
                                <th>status</th>
                                <th>Cancel Product</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (orders && orders.length> 0) { %>
                                <%  orders.forEach((order,i)=> { %>
                                    <% order.products.forEach((product) => {  %>
                            <tr>
                                <td>
                                    <%= order.orderNumber %>
                                </td>
                                <td>
                                    <%= product.product_name %>
                                </td>
                                <td>
                                    ₹<%= product.price %>
                                </td>
                                <td>
                                    <%= product.quantity %>
                                </td>
                                <td>₹<%= orders[i].totalAmount %></td>
                                <td>
                                     <!-- Use if-else conditions to determine the class -->
                                     <% let statusClass='' ; %>
                                     <% if (order.status==='Pending' ) { %>
                                         <% statusClass='btn-warning' ; %>
                                             <% } else if (order.status==='Processing' ) { %>
                                                 <% statusClass='btn-primary' ; %>
                                                     <% } else if (order.status==='Shipped' ) { %>
                                                         <% statusClass='btn-info' ; %>
                                                             <% } else if (order.status==='Delivered' ) { %>
                                                                 <% statusClass='btn-success' ; %>
                                                                     <% } else if (order.status==='Cancelled' ) {
                                                                         %>
                                                                         <% statusClass='btn-danger' ; %>
                                                                             <% } else { %>
                                                                                 <% statusClass='btn-secondary' ;
                                                                                     %>
                                                                                     <% } %>
                                                                                     
                                                                                         <button
                                                                                             class="btn <%= statusClass %> btn-outline-dark" id="statusButton">
                                                                                             <%= order.status %>
                                                                                            
                                                                                    
                                                                                         </button>
                                                                                         <% if (order.status==='Delivered' ) { %>
                                                                                            <a class="btn btn-dark" href="/viewInvoice?orderNumber=<%= order.orderNumber %>">View Invoice</a>
                                                                                            <%} %>
                                </td>
                                <td>
                                    <% if (order.status !=='Cancelled' ) { %> 
                                <div
                                    style="display: flex; justify-content: center; padding-top: 20px; padding-bottom: 20px;">
                                    <button class="btn btn-primary btn-outline-dark "  id="cancelOrderButton"
                                            data-order-number="<%= order.orderNumber %>">Cancel Order</button>
                                </div>
                                <% } else { %> 
                                    <img src="https://orig14.deviantart.net/ea10/f/2009/201/3/f/crappy_animation___dog_blink_by_darkchocaholic.gif" alt="" style="height: 50px; width: max-content;">
                                    <%} %>
                                </td>
                            </tr>
                            <% }) %>
                            
                            <% }) %>
                            <% } %>
                        </tbody>
                        <tfoot>
                            
                        </tfoot>
                    </table>        
            </div>
        </div>

        

    </div>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <script src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap4.min.js"></script>
    <script>
        new DataTable('#example');
    </script>
    <script>
    const cancelOrderButton = document.getElementById("cancelOrderButton");

    cancelOrderButton.addEventListener("click", () => {
        const orderNumber = cancelOrderButton.getAttribute("data-order-number")
        Swal.fire({
            title: "Are you sure?",
            text: "You won't be able to revert this!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#3085d6",
            cancelButtonColor: "#d33",
            confirmButtonText: "Yes, cancel it!",
        }).then((result) => {
            if (result.isConfirmed) {
                console.log('hello at order ejs ')
                        // Make an asynchronous request to the backend
                        // sending the order number
                fetch(`/cancelOrder/${orderNumber}`, {
                            method: "POST",
                            headers: {
                                "Content-Type": "application/json",
                            },
                            // You can include additional data in the body if needed
                        })
                            .then((response) => {
                                if (response.ok) {
                                    return response.json();
                                }
                                throw new Error("Network response was not ok.");
                            })
                            .then((data) => {
                                console.log(data); // Handle the response from the backend

                                // check if the cancellation was successful
                                if (data.success) {
                                    // Update the button text to 'Cancelled'
                                    statusButton.innerHTML = 'Cancelled'

                                    // Add a class to change the button style
                                    statusButton.classList.add('btn-danger')

                                    // Hide the "Cancel Order" button
                                    cancelOrderButton.style.display = 'none';

                                }
                            })
                            .catch((error) => {
                                console.error("There was a problem with the fetch operation:", error);
                            });


                // Handle the cancellation logic here
                Swal.fire("Order Cancelled!", "Your order has been cancelled.", "success")
                    
            }
        });
    });
</script>



    <script>
        setTimeout(function () {
            const footer = document.getElementById('myFooter');
            footer.style.display = 'block';
        }, 3000); // 1000 milliseconds (1 second)
    </script>
    <script>
        // Function to animate the progress bar
        function moveProgressBar() {
            const progress = document.getElementById('progress')
            const progressBar = document.getElementById('progress-bar');
            const message = document.getElementById('message');
            let width = 0;

            // Increment the width in steps and update the progress bar
            const interval = setInterval(function () {
                if (width >= 148) {
                    clearInterval(interval);
                    // Hide the striped progress bar
                    progressBar.style.display = 'none';
                    progress.style.display = 'none'

                    // Use setTimeout to display "Hello World" after 2 seconds
                    setTimeout(function () {
                        message.style.display = 'block';
                        message.class = 'fly-in'
                    }, 1); // 2000 milliseconds (2 seconds) delay
                } else {
                    width++;
                    progressBar.style.width = width + '%';
                    progressBar.setAttribute('aria-valuenow', width);
                }
            }, 20); // Adjust the duration of each step (milliseconds)
        }

        // Call the function to start the progress animation
        moveProgressBar();
    </script>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <!-- Include SweetAlert library -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script> -->

    <% if (locals.userIsBlocked) { %>
        <script>
            // Display SweetAlert when the user is blocked
            Swal.fire({

                icon: 'error',
                title: 'Account Blocked',
                text: 'Your account has been blocked. Please contact support.',
                showCancelButton: false,
                confirmButtonText: 'Ok',
                timer: 2000,
            }).then(() => {
                // Redirect to the login page
                window.location.href = '/userBlockedByAdmin';
            });
        </script>
        <% } %>

</body>

</html>