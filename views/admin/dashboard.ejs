<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paws&Claws</title>
    <link rel="stylesheet" href="<%-url%>/css/output.css">
</head>
<body class="bg-gray-200  p-4 sm:p-6">
    <div class="container  mx-auto">
        <h1 class="text-2xl font-semibold mb-2 sm:mb-4">Admin Dashboard</h1>
        <div class="bg-white p-2 sm:p-4 rounded shadow-md overflow-x-auto">
            <table class="w-full whitespace-nowrap sm:table-responsive">
                <thead>
                    <tr>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">User ID</th>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">Username</th>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">Phone</th>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">Role</th>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">Status</th>
                        <th class="px-2 py-1 sm:px-4 sm:py-2">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Example user data -->
                    <tr>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2">1</td>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2">User123</td>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2">123-456-7890</td>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2">User</td>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2">Active</td>
                        <td class="border px-2 py-1 sm:px-4 sm:py-2 d-flex justify-content-between">
                            <button class="bg-blue-500 hover:bg-blue-600 text-white px-2 py-1 rounded mr-1 sm:mr-2">Make Admin</button>
                            <button class="bg-red-500 hover:bg-red-600 text-white px-2 py-1 rounded">Block User</button>
                        </td>
                        
                    </tr>
                    <!-- Repeat the above row for each user -->
                   
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
